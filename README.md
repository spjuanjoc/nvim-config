# nvim-config

Install

```shell
sudo apt install -y neovim
```

Health

```
neovim +checkhealth
```

## Setup

Create directory

```
cd ~/.config/
mkdir nvim
```

Clone this repo in `~/.config/nvim`

Copy the file `init-vim` to `~/.config/nvim`
